# Production/cPanel: skip devDependencies (nodemon). UI build: npm run build:ui (uses --include=dev for frontend).
omit=dev
audit=false
fund=false
engine-strict=false
# cPanel/CloudLinux: use the app virtualenv Node for lifecycle scripts (not system Node 10)
scripts-prepend-node-path=true
