# ─── PRODUCTION .env for localhost:3004 ──────────────────────────────────
# Copy/upload this file to the server as ".env" (overwrite the existing one)
# then restart the app from cPanel → Setup Node.js App.
#
# DO NOT set PORT here — Passenger assigns it automatically.

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Hellt0cell
DB_NAME=hms_demo

NODE_ENV=production
SESSION_SECRET=hms-prod-please-replace-with-a-long-random-string
